Returns all records from the right table and the matched records from the left table. Non-matching rows in the left table are filled with NULL.

Example:

sql
SELECT A.*, B.*
FROM TableA A
RIGHT JOIN TableB B ON A.id = B.a_id;
